services:
  db:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
      POSTGRES_DB: events
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
